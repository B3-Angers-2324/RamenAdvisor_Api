import {describe, expect, test} from '@jest/globals';

describe('Main', () => {
    //Login test
    test('Fill the void', async () => {
        let a = 1;
        expect(a).toBe(1);
    });
        
});
